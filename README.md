# Simulation-based-Evaluation-of-a-Network-Interface-Card-with-Offload-Capability

A jarfile(package) has to be made for all the classes before running.

Running Simulator
------------------
java -jar [jar name] [value of arrival rate]
